name: flutter_fire_chat
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

  firebase_auth: ^0.11.1+7
  provider: ^3.0.0+1
  google_sign_in: ^4.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_automation:
  
flutter:
  uses-material-design: true